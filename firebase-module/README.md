# Firebase Module

Reusable Firebase Admin module.

What it keeps from the source servers:

- Singleton initialization and newline-safe private key handling from both servers.
- Clear token verification error mapping from `server-2`.

It is optional and does not couple the backend template to Firebase.

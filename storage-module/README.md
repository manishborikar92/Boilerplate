# Storage Module

Reusable object storage adapter module.

What it keeps from the source servers:

- Cloudinary upload/delete/metadata/signed URL coverage from `server-2`.
- Centralized upload/storage configuration idea from `server-3`.

It does not define project-specific folders such as shops, documents, customers, or profile photos.
